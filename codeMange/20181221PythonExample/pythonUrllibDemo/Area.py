class Area:

    def __init__(self, self_name, self_url):
        # print("初始化地区名字:%s 地区url:%s" % (self_name, self_url))
        self.name = self_name
        self.url = self_url

    def setUrl(self, self_codel):
        # print("初始化code：%s" % self_codel)
        self.code = self_codel